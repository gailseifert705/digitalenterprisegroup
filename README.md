# Digital Enterprise Group Protocol Projects

Run locally:

```powershell
cd "C:\Users\HP\Downloads\digital-enterprise-group-protocol-projects-clean"
npm install --no-audit --no-fund
npm run dev
```

This cleaned package does not include package-lock.json so npm will use the public npm registry on your computer.
